# We can use print()function as many times as you want
# Each call prints text on a new line by default:
print("Hello World")
print("I am learning Python.")
print("It is awesome!")
#text in Python must be inside quotes. We can use either " " double quotes or ' ' single quotes:
print("This will work!")
print('This will also work!')
# Quotes are mandatory for text if we don't put the text inside the quotes it will throw an error.

